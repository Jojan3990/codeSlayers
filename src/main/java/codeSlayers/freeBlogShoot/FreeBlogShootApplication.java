package codeSlayers.freeBlogShoot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
//tomcat is s server which is running or hosting
@SpringBootApplication
public class FreeBlogShootApplication {

	public static void main(String[] args) {

		SpringApplication.run(FreeBlogShootApplication.class, args);
	}
}
